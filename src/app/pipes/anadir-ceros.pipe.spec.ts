import { AnadirCerosPipe } from './anadir-ceros.pipe';

describe('AnadirCerosPipe', () => {
  it('create an instance', () => {
    const pipe = new AnadirCerosPipe();
    expect(pipe).toBeTruthy();
  });
});
